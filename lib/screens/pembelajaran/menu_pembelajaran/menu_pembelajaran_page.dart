import 'package:eco_quiz/widgets/app_bar.dart';
import 'package:flutter/material.dart';

class MenuPembelajaranPage extends StatelessWidget {
  const MenuPembelajaranPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: appBar("Pembelajaran"),
      body: ListView(
        
      ),
    );
  }
}
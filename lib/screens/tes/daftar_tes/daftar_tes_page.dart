import 'package:flutter/material.dart';

class DaftarTes extends StatelessWidget {
  const DaftarTes({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(),
    );
  }
}